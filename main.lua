local  composer = require("composer")
local scene = composer.newScene()

display.setStatusBar( display.HiddenStatusBar )	--hide the status bar through out the game

composer.gotoScene("menu")	--Start the game with the scene "Opening_scene"